# 当前状态

> 本文件是项目当前状态的唯一短入口；产品定义仍以 `docs/需求与方案.md` 为准，历史过程见 Git 与 `tasks/progress.md`。

## 当前阶段

多会话在途内容已统一收口。其他相关会话已归档或确认非活动；共享工作区恢复为单 owner 模式。下一阶段冻结新增 UI、连接器、知识库、部署路线和视频形态，先完成真实学习效果验证。

## 唯一下一门槛

使用既有 20 条素材完成裸读/裸看基线与同批速听对照，复算速度、一致率、必看漏判、保真和 4/5 复述指标。

## 活跃任务

| 任务 | Owner | 分支/工作区 | 文件范围 | 状态 |
|---|---|---|---|---|
| 多会话在途内容统一收口 | 当前主会话 | 当前共享工作区 | 全工作区分类、备份、恢复、验证与提交 | 已完成 |
| 恢复昨日已验收的线上私有版本 | 当前主会话 | 当前共享工作区 | 私有邮件批次、固定音频、历史/重点讲部署入口 | 已完成 |

## 并发规则

- 新会话使用独立 `codex/<task>` 分支和独立 Git worktree，不直接共享当前工作区。
- 开工前在本表登记 owner；同一文件同一时间只允许一个 owner。
- `tasks/todo.md` 与 `tasks/progress.md` 仅保留历史，不再由多个会话重复追加全局状态。
- 真实邮件、每日音频、历史和 usage 缓存只写入 Git 忽略的 `local-data/`。

## 本轮冻结

- 已归档旧“资讯速听设计”会话；早期“接管项目”会话为未加载/非活动状态，没有运行任务。
- 在本轮完成并提交前，不允许其他会话继续修改本项目。
- 私人邮件派生 JSON/音频先备份到 `local-data/inflight-snapshots/`，绝不进入公开 Git。
- 尚未确认的页面导航与视觉改动保存为本地 patch，退出工作区，后续单独评审。

## 本地保留（不提交）

- `local-data/inflight-snapshots/2026-07-17-prototype-private-content/`：真实邮件派生资讯包与音频快照。
- `local-data/inflight-patches/deferred-ui-2026-07-17.patch`：尚未确认的导航与跨页面视觉改稿。
- `local-data/inflight-patches/concurrent-task-logs-2026-07-17.patch`：并发会话原始任务日志差异，仅供追查。

## 后续迭代顺序

1. 完成 20 条真实素材的裸读/速听对照，先证明快速判断层有效。
2. 用工程教程、模型解读、产品评测、访谈观点补齐四类课程验证。
3. 前两项通过后再评估“速听 → 课程”的深链路，不提前扩展知识库和连接器。
4. 基于 3–5 次真实 usage 台账调整输出上限，并评估 TTS 内容寻址缓存。
5. 延后 UI patch 仅在新的明确设计任务中重新评审，不自动恢复。
