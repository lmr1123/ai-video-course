# Todo

> 本文件保留历史执行记录；当前阶段、唯一 owner 和下一门槛统一见 `tasks/current.md`。

## 2026-07-17 · 多会话在途内容统一收口

范围：停止共享工作区的并发修改，区分可提交代码、本地私有产物和后续产品迭代，避免过期状态与真实内容进入仓库。

- [x] 盘点相关会话与共享工作区改动
- [x] 将真实邮件派生内容、音频和未确认 UI 改稿移出 Git 候选
- [x] 保留模型调用预算、缓存、usage 台账和本地历史能力
- [x] 完成全量测试、敏感信息扫描并提交

### Review

- 当前主会话为唯一 owner；旧设计会话已归档，另一早期会话为非活动状态。
- 本地私有快照与延后 patch 保存在被忽略的 `local-data/inflight-*`，不进入 Git。
- 后续顺序以 `tasks/current.md` 为准，不从历史日志推断当前状态。
- 验证：43 项单元测试通过；资讯速听页在 1280×900 与 390×844 的 Chrome 烟测通过；`git diff --check` 与敏感信息扫描通过。

## 2026-07-16 · 资讯速听页简洁化

范围：按用户反馈和 `emilkowalski/skills.git` 设计参考，优化 `prototype/briefing/index.html` 的阅读页面；重点减少横线和硬边界，不改资讯生成管线、不新增功能。

成功标准：

- [x] 安装或读取用户指定 skill，并提炼本轮可用的简洁设计约束
- [x] 减少页面非功能性横线与过重边框，改用留白、字号、字重和轻底色建立层级
- [x] 保留播放、队列、逐句高亮、收藏、原文和导入内容包功能
- [x] 完成语法检查与桌面/手机浏览器烟测

### Review

- 已安装并读取 `emil-design-eng` skill；本轮采用其“高频界面减少无目的动效、边界只服务功能、按钮保留轻微按压反馈”的设计约束。
- `prototype/briefing/index.html` 去掉证据胶囊和判断区大容器，改为开放正文、轻文字链接、留白和字重层级；底部时间线改成一条连续进度轨，减少碎线感。
- 验证：`python3 -m py_compile tools/briefing_pipeline.py tests/briefing_browser_smoke.py` 通过；`python3 tests/briefing_browser_smoke.py` 在 1280×900 和 390×844 通过，确认播放、队列、收藏、导入、音频资源和无横向溢出。

## 2026-07-16 · Gmail 发件人白名单接入（用户确认提前启用邮件通道）

范围：用户确认用 IMAP + 应用专用密码接入 Gmail，按发件人白名单拉信做资讯播报。凭据只走环境变量，邮件产物只进被忽略的 `local-data/briefing/mail-inbox/`。

- [x] 实现 `tools/gmail_ingest.py`：IMAP 拉取 → 头部解码 → 纯文本优先/HTML 降级 → 段落锚点 → Gmail 原文链接 → 验证：合成邮件单测与无凭据错误路径通过
- [ ] 用户生成应用专用密码并设置 `GMAIL_ADDRESS` / `GMAIL_APP_PASSWORD`，提供发件人白名单
- [x] 首次真实拉取 → 在会话内提炼成 briefing.json → 走现有 `briefing_pipeline.py --tts` 播报验收

### Review

- 脚本只做拉取与标准化，不含 LLM 提炼；段落 index 即 `email_paragraph` 锚点 locator，输出可直接供提炼成现有 schema。
- 已验证：合成多部分邮件的主题解码、纯文本优先、HTML 剥离 script/style、段落切分、rfc822msgid 永久链接；缺凭据时友好报错退出。
- 真实链路已跑通：`swyx+ainews@substack.com` 近7天 5 封全部完整提取；会话内提炼成 4 张速听卡（GPT 5.6 发布与落地聚合为同一事件卡）、15 段、总时长 3.5 分钟；schema 校验、edge-tts 生成、HTTP 200 冒烟（页面/JSON/音频）全部通过。
- 未验证边界：浏览器可视回归与真人连续听感未做（本会话安全分类器被 GLM 环境泄漏卡死，见 lessons）；`.env` 中仍是聊天中暴露过的应用专用密码，待用户更换。
- 环境修复：GLM 模型映射与代理 token 再次泄漏进 `~/.claude/settings.json` env，已按 2026-07-12 规范移回 opt-in `~/.claude/glm-env.sh`，需重启客户端生效。

## 2026-07-15 · 多源 AI 资讯速听模式设计

目标：设计一个接收粘贴文本、链接、邮件、图片、音视频等输入的“快速判断层”，把多条 AI 资讯压缩成可连续收听且同步可扫读的短内容，并让用户能从任一判断追溯原文、继续深挖或进入现有课程学习流程。本轮先做产品方案与竞品/开源调研，不实现代码，不直接修改产品基线。

### 计划

- [x] 校准与现有课程导读、中文播放、AI 重讲的关系 → 验证：职责不重叠，升级路径明确
- [x] 调研现有商业产品与开源项目 → 验证：覆盖多源导入、提炼、音频队列、溯源与继续探索能力
- [x] 设计端到端消费流程和最小内容 schema → 验证：输入、处理、判断、输出、边界完整
- [x] 定义首版范围与真实样例验证协议 → 验证：能测量获取速度、要点保真、兴趣判断和原文追溯
- [x] 整理需求背景、产品方案、关键教训、工具地图、进度与下一步 → 验证：形成一份明确标注“待确认”的独立方案文档
- [x] 向用户确认定位与关键取舍后，再更新 `docs/需求与方案.md` 和 `docs/决策日志.md`

## 2026-07-15 · 多源资讯速听首轮执行

范围：按已确认方案正式落档产品定义与决策，准备 20 条真实验证素材，并实现“手工投递 → 可追溯短卡 → 连续语音/同步文字 → 原文或深挖”的最小本地原型。保持现有未完成中文音轨改动不受影响。

成功标准：正式文档与待确认方案一致；样本覆盖来源、重复事件和高风险表达；原型只实现首版范围；确定性测试和桌面/手机浏览器回归通过；在 Review 中明确真实内容质量、TTS 与外部抓取的证据边界。

- [x] 建立持久设计上下文 → 验证：目标用户、场景、界面语气和反模式明确
- [x] 更新正式产品基线与决策日志 → 验证：资讯速听进入产品结构且边界无冲突
- [x] 建立 20 条真实验证素材清单与基线记录模板 → 验证：覆盖 8 类内容、重复事件和高风险项
- [x] 检查现有生成、播放器和测试结构，确定最小复用点 → 验证：不破坏课程与中文音轨链路
- [x] 实现最小生成管线与本地资讯速听页 → 验证：fixture 可生成短卡、证据锚点和连续播放数据
- [x] 完成确定性测试与桌面/手机浏览器回归 → 验证：导入、播放、跳转、收藏和深挖路径可用
- [x] 补充 Review → 验证：记录完成范围、测试证据、未验证边界和下一步门槛

### Review

- 正式产品基线已加入“资讯速听 → 课程学习”两段式路径；决策日志记录了快速判断层、手工输入、单人快讲和追溯边界。
- 新增 `.impeccable.md`，把目标用户、低视觉占用场景、紧凑可信安静的品牌语气和浅色编辑台视觉固化为项目设计上下文。
- 20 条真实素材与裸读基线模板位于 `experiments/资讯速听首轮验证/素材与基线.md`，覆盖 8 类内容、5 条同事件重复和高风险表达；尚未执行真人基线。
- 新增 `tools/briefing_pipeline.py`、fixture 和 7 项资讯速听单测：校验来源、事实/观点/推断归属、证据锚点、事件聚合和安全链接；可选按 spoken segment 生成并缓存 edge-tts MP3。
- 新增 `prototype/briefing/index.html`：逐句连续播放与高亮、浏览器语音降级、队列切换、变速、收藏、原文、深挖和本地 JSON 导入；私有产物继续进入被忽略的 `local-data/briefing/`。
- 验证证据：21 项全量单元测试、Python 编译、播放器 JavaScript 语法、fixture 写出和 `git diff --check` 通过；Chrome 1280×900 与 390×844 回归通过，无横向溢出或控制台错误。
- 真实边界：本轮没有调用模型自动提炼 20 条内容，也没有执行真实 Edge TTS 长听；fixture 证明结构和交互可运行，不能证明真实摘要保真、判断一致或听感指标通过。

### Review（已确认并进入执行）

- 建议定位为现有产品的“多源资讯快速判断层”，负责跨来源批量筛选；课程导读和深讲继续负责单条长视频的理解与迁移。
- 市场已有多源收件箱、AI 摘要、音频概览和新闻语音流等单点产品，但缺少“保留单条边界的短语音卡＋连续队列＋句级溯源＋一键深挖”的完整闭环。
- 首版建议只做手动投递和文件导入，用真实 AI 新闻、X 帖/截图、邮件、播客或视频验证提炼质量；RSS、邮箱授权、自动订阅和黑箱个性化后置。
- 默认每条 20—60 秒、单人自然声线，文字与语音共用同一脚本；事实、作者观点和 AI 判断必须分型，并绑定原文段落、图片区域或音视频时间戳。
- 2026-07-15 用户确认定位、首版输入边界和单人快讲形态；正式落档与原型执行记录见下方“多源资讯速听首轮执行”。

## 2026-07-12 · 三模块连续迭代

目标：完成深讲阅读优化、把首课验收收缩为一次闭卷复述，并建立从 YouTube 链接到可部署课程页的本地自动生成管线。

### 1. 深讲浅色卡片

- [x] 四个深讲模块的 Q1–Q6 改为低对比浅色卡片，全文默认可见
- [x] 去掉卡片内嵌套卡片，重新建立引文、实践和归属层级
- [x] 桌面与 390px 手机视觉回归

### 2. 首课闭卷复述验收

- [x] 课程末尾增加一个闭卷复述区，只问“请用自己的话复述本课核心机制”
- [x] 提交前隐藏参考要点；提交后显示四项自检标准，不调用模型
- [x] 使用 localStorage 保存用户输入，刷新不丢失
- [ ] 等用户真实填写后，根据原视频依据判断是否通过

### 3. 本地自动课程生成管线

- [x] `YouTube URL → yt-dlp 字幕/元数据 → 标准化转录`
- [x] 使用 OpenAI Responses API 结构化生成导读、5–12 个主题和 2–3 个深讲模块
- [x] 校验主题顺序、时间戳范围、内容归属和必填字段；失败不写半成品
- [x] 输出课程 JSON 与独立手机课程页，并生成课程索引供根页面识别
- [x] 用现有 Codex 访谈字幕做无 API fixture 回归
- [ ] 配置 `OPENAI_API_KEY` 后做一次真实端到端模型生成
- [x] 更新 README、决策日志和运行说明

### 完成标准

- 卡片页与复述区通过本地和公网手机回归；
- 管线无需手工改 HTML，给定 URL 后能生成可由 GitHub Pages 托管的静态课程；
- API 密钥只从环境变量读取，绝不进入仓库或前端；
- 生成结果仍满足课程导读、知识地图、深讲和时间戳追溯四项 MVP 边界。

### Review

- 卡片：四个模块共 24 张通俗讲解卡片，背景 `#fdfaf4`，全文可见；引文与实践改为卡片内分隔区，不嵌套卡片。
- 复述：提交前隐藏四项参考点，提交后显示；答案写入按视频区分的 localStorage。
- 管线：真实字幕获取已验证（70 分钟视频标准化为 137 个分析段）；3 项 schema/写出测试通过；缺少 API Key 时明确失败且不写半成品。
- 通用课程页：fixture 生成 5 个主题、2 个深讲、16 张内容卡；390px/1280px 均无溢出和页面错误。
- 公网：主课与通用课程页均为 HTTP 200；390px 下卡片、复述和课程 JSON 加载无溢出、无页面错误。
- 未完成：需要用户真实提交复述；需要配置 `OPENAI_API_KEY` 后跑第二条视频验证模型输出。

## 2026-07-12 · 深讲模块快速阅读优化

范围：不删减、不折叠课程内容，用低对比浅色卡片把四个深讲模块的每个问题分成独立阅读单元。

成功标准：

- 通俗讲解仍默认显示且全文可见，无额外点击成本；
- 每个问题使用低对比浅色卡片分组，标题和正文层级一眼可辨；
- 引文、案例和最小实践在卡片内保持清晰，但不形成嵌套卡片；
- 桌面与 390px 手机均为易读单列，时间戳和深度切换保持正常；
- 部署后公网回归通过。

计划：

- [x] 设计浅色问题卡片的背景、间距、标题与正文层级
- [x] 应用到四个深讲模块，保留全文连续可见和现有交互
- [x] 修正深讲区现有重复结构并完成桌面、手机回归
- [x] 部署并完成公网验证

### Review

- 24 张问题卡片使用统一浅色表面与细边框，正文默认全部可见。
- 引文和实践使用卡片内横线分隔，不再形成嵌套卡片。
- 本地与公网 390px/1280px 回归无溢出；主课 24 张卡片完整可见。

## 2026-07-12 · 修正双语字幕遮挡

范围：保留 YouTube 原生英文字幕，页面覆盖层仅显示短句中文字幕，解决长段双语字幕遮住视频的问题。

成功标准：英文由 YouTube 字幕轨显示；中文单条为短句、最多两行；播放与跳转同步保持正常；手机与桌面画面不被大段字幕覆盖。

- [x] 将中文长段按标点与持续时间拆成短 cue
- [x] 移除英文覆盖层并启用 YouTube 原生英文字幕
- [x] 在多个时间点和 390px 手机视口验证字幕长度与遮挡
- [x] 部署并完成公网回归

### Review

- 159 个转录长段拆成 1,489 个中文短 cue，平均 15.9 字，最长 25 字。
- 英文改由 YouTube 原生 `en` 字幕轨显示；页面只在视频下方黑色窄条显示中文，不再覆盖原画面。
- 390px 与桌面抽查 5 个跨全片时间点：中文字幕高度固定为 38/42px，无横向溢出、零控制台错误。
- 手机播放时不再自动滚走视频；知识地图仍会更新和展开当前项。
- 公网 390px 验证：1,489 个 cue 加载成功，760s 显示 14 字中文短句，字幕条 38px，页面未滚动、无溢出和控制台错误。

## 2026-07-12 · 双语字幕与知识地图自动展开

范围：增强现有互动课程页，不改课程生成管线。为当前样例提供随播放同步的字幕能力，并让知识地图当前主题自动展开核心内容描述。

成功标准：

- 播放、跳转或拖动后，字幕与当前播放时间同步；
- 用户可关闭字幕，并按确认的模式查看中英文；
- 仅当前知识地图条目自动展开核心判断与学习价值，切换主题后旧条目收起；
- 手机宽度无横向溢出，播放同步、时间戳跳转和重讲入口保持正常；
- GitHub Pages 更新后公网验证通过。

计划：

- [x] 整理当前样例的时间轴字幕数据与中文对应文本 → 验证：首、中、尾时间点命中正确字幕
- [x] 实现播放器双语字幕层和显示控制 → 验证：播放/跳转/关闭状态
- [x] 扩充知识地图核心描述并实现当前项自动展开 → 验证：15 个主题逐段切换只展开一项
- [x] 完成桌面与 390px 手机回归并部署 → 验证：本地与公网冒烟测试

### Review

- 字幕：缓存 159 段英文原文与简体中文机器翻译，英文在上、中文在下；播放阶段 0 Token。
- 知识地图：当前主题自动展开“核心＋价值”，播放切换时旧主题收起并滚动到可见区域。
- 本地验证：180s、760s、1920s、4030s 四个时间点字幕与主题均正确命中；桌面和 390px 手机均只展开一项、无横向溢出、零控制台错误。
- 边界：字幕来自 YouTube 自动字幕及机器翻译，专有名词仍可能需要人工校正。
- 公网验证：页面与 159 段字幕加载成功；760s 命中主题 3，仅展开一个详情，390px 无溢出、零控制台错误。

## 2026-07-12 · 移动端可访问部署

范围：把当前静态课程原型部署到 GitHub Pages，提供手机可直接打开的公网地址；保留现有课程、重讲模式与音频资源，不引入应用框架或后端。

成功标准：

- 手机宽度下主课程页和重讲页无横向溢出，播放器、导航和主要按钮可操作；
- GitHub Pages 能从 `main` 自动发布 `prototype/`；
- 公网首页、重讲页及音频资源返回成功状态；
- 部署方式和访问地址记录到 README 与本文件 Review。

计划：

- [x] 检查主课程页与重讲页的移动端布局、资源引用和浏览器错误 → 验证：390px 视口冒烟测试
- [x] 增加最小 GitHub Pages 发布配置并更新项目说明 → 验证：工作流语法与静态资源路径检查
- [x] 整理并提交当前迭代，推送到现有仓库 → 验证：远端提交与本地一致
- [x] 开启 GitHub Pages 并等待发布完成 → 验证：公网 URL、重讲页、音频资源均可访问

### Review

- 部署：GitHub Pages；仓库已按用户确认从私有改为公开，Actions 从 `prototype/` 自动发布。
- 地址：`https://lmr1123.github.io/ai-video-course/`；重讲页为 `/relecture/`。
- 验证：两个 HTML 页面和首个 MP3 均返回 HTTP 200；Chrome 390×844 视口无横向溢出、零控制台错误，主课程成功加载，重讲页渲染 17 个分镜。
- 提交：课程迭代与部署配置见 `ee04574`。

# 2026-07-13 · 中文播放模式设计

范围：为不会英文、且不希望持续阅读中文字幕的用户设计“中文播放”体验；先完成产品与交互方案，不实现页面、配音生成或发布流程。

成功标准：方案能说明它解决的学习障碍、进入与使用时机、与原声字幕/中文助讲/AI 重讲的边界、音画与时间戳追溯方式、开源后的生成成本与版权边界，并给出可用真实视频验证的判定标准。

- [x] 梳理现有播放、字幕、助讲与重讲模式，确认中文播放的最小职责 → 验证：无功能重叠或概念冲突
- [x] 设计中文音轨的入口、播放状态、同步策略与异常降级 → 验证：用户可以不盯字幕完成连续收听，且能随时核对原片
- [x] 定义内容生成、术语校正、声音呈现和开源配置边界 → 验证：输入、过程、判断方式、输出与限制完整
- [x] 制定真实视频验证方案并收敛首版范围 → 验证：覆盖理解、同步、追溯、术语、听感和成本指标
- [x] 将确认后的产品选择更新到需求与决策文档，并在本节补充 Review

### Review

- 中文播放被定义为原片模式下的翻译音轨，与插入式助讲、教学重组重讲明确分轨。
- 同步以 3—12 秒语义句为单位；中文过长时优先口语压缩，其次调节 TTS 与利用停顿，局部画面最低 0.9×，不允许跨概念抢跑。
- 首版只验证现有访谈样例的 8—12 分钟片段，不直接生成 70 分钟全片；通过后再覆盖另外三类视频。
- 开源仓库不打包第三方视频、原音频、完整字幕或合成中文音轨，默认由用户本地生成和缓存。
- 开源版必须提供完整本地生成能力；用户 `git clone` 后可生成字幕与中文音轨，产物统一进入 `local-data/`，不被 Git 跟踪或自动发布。

# 2026-07-13 · 本地中文播放开发实现

范围：实现用户克隆项目后，用自己的环境从 YouTube URL 生成本地中文字幕和原片同步中文音轨，并在本地播放器中切换中文/原声、拖动和核对原话。首次真实验证只生成连续 10—20 分钟；只做个人本地使用链路，不新增云端服务或公开发布能力。

成功标准：一条命令可以生成符合 schema 的 `local-data/<video-id>/`；离线 fixture 可跑通无网络测试；本地播放器能驱动 YouTube 原画与分句中文音频同步；用户拖动、暂停、切换音轨后状态正确；第三方产物不会被 Git 跟踪。

- [x] 拆出适合配音的短字幕 cue，并定义可校验的翻译音轨 schema → 验证：3—12 秒时间段有序、无重叠、字段完整
- [x] 实现 URL → 字幕 → 批量中文口语翻译 → 分句 edge-tts → `local-data/` → 验证：fixture 与真实 CLI 错误边界
- [x] 实现本地中文播放器和中文/原声/核对原话交互 → 验证：播放、暂停、拖动、变速、字幕和失步恢复
- [x] 增加本地启动入口、依赖与 README 操作说明 → 验证：新用户按文档能运行且密钥/产物不入 Git
- [x] 运行单元测试、静态检查和桌面/手机浏览器回归 → 验证：现有测试不回退，新链路关键场景通过
- [x] 在本节补充 Review，记录实际完成范围、命令、测试证据与剩余边界

### Review

- 实现入口：`python3 tools/dubbing_pipeline.py URL --start 00:02:58 --minutes 10 --serve`。
- 真实样例：`P3KDebPTUrw` 的 02:58—12:58，共 79 条无重叠 cue、79 个本地 MP3，全部通过 1.35× 时长上限。
- 自动验证：11 项单元测试、Python 编译、播放器 JavaScript 语法、10—20 分钟参数限制、79 个音频文件和 gitignore 检查全部通过。
- 浏览器验证：桌面与 390px 无横向溢出；中文同步、原声切换、核对原话、跳转和变速通过。
- 未验证：本机缺少 `OPENAI_API_KEY`，真实批量翻译调用和翻译内容质量待配置 Key 后补测；本轮中文稿来自项目既有缓存字幕并对 15 条越界句做了人工压缩。

# 2026-07-13 · 访谈双角色音色与音轨发布边界

范围：为访谈中文音轨增加主持人/嘉宾角色，按真实试听选择的 `Yunxi` 音高和语速区分角色；重生成并验证现有 10 分钟片段。同时明确开源仓库对不同来源音轨的提交规则。

成功标准：manifest 每条 cue 可追溯到 speaker；自动角色判断可被本地 overrides 修正；两种声音按角色生成且播放器显示当前角色；现有 10 分钟样例角色切换可听辨；仓库不会仅因“非商用”而默认发布无授权音轨。

已确认音色：主持人使用 `zh-CN-YunxiNeural`，`pitch=-4Hz`，`rate=+2%`；嘉宾使用 `zh-CN-YunxiNeural`，`pitch=-10Hz`，`rate=-2%`。

- [x] 扩展 speaker schema、翻译输出和本地覆盖机制 → 验证：角色、voice、rate、pitch 均可校验
- [x] 更新 TTS 缓存签名与播放器角色呈现 → 验证：换角色或声音会重生成对应音频
- [x] 让 TTS 重生成可从临时网络故障恢复 → 验证：有限重试且新音频成功前保留旧缓存
- [ ] 人工核对并重生成 10 分钟双角色样例 → 验证：主持人/嘉宾切换正确且全部音频符合时长上限
- [x] 将可提交音轨范围写入方案与 README → 验证：自有/授权/CC BY 可提交，标准许可第三方默认仅本地
- [ ] 运行单测、浏览器回归并补充 Review

### Review（进行中）

- 角色 schema、自动识别、本地 overrides、角色参数化 TTS、播放器角色显示均已实现；主持人默认 `Yunxi / -4Hz / +2%`，嘉宾默认 `Yunxi / -10Hz / -2%`。
- TTS 改为临时文件成功后原子替换，单句失败有限重试；14 项单元测试、Python 编译、播放器 JavaScript 语法、HTTP 路径、diff 和 gitignore 检查通过。
- 第三方音轨发布边界已写入 README、方案和决策日志：“非商用”不等于自动授权，普通第三方内容默认只留本地。
- 真实 10 分钟样例因 Edge TTS 间歇连接超时生成到 37/79，37 段均未超 1.35× 时间窗；其余 42 段标为 `missing`，重跑同一命令会复用翻译并从缺失音频继续。
# 2026-07-15 · 发布资讯速听与中文播放迭代

范围：把已完成并记录的中文播放、双角色音轨和多源资讯速听改动整理到 Git，推送到现有 GitHub 仓库，并确认 GitHub Pages 上的资讯速听页面可供手机验收。

成功标准：仅提交本轮项目内已记录的文件；确定性测试通过；远端提交与本地一致；`/briefing/` 公网页面返回成功且手机宽度无明显回归。

- [x] 核对工作区差异与发布范围 → 验证：所有待提交文件都能追溯到已记录任务
- [x] 运行资讯速听、中文播放及现有课程核心测试 → 验证：测试全部通过
- [x] 提交并推送现有 GitHub 仓库 → 验证：远端 commit 与本地 HEAD 一致
- [x] 等待并检查 GitHub Pages → 验证：手机可访问 `/briefing/`
- [x] 补充 Review，记录提交、地址、验证结果与已知边界

### Review

- 功能提交：`ecf12aa`，已推送到 `origin/main`；GitHub Pages 工作流 `29406695277` 完成且结论为 `success`。
- 公网地址：`https://lmr1123.github.io/ai-video-course/briefing/`，部署后返回 HTTP 200，线上文件长度与本地原型一致。
- 验证：资讯速听、课程生成、中文播放共 21 项单元测试通过；Python、内嵌 JavaScript、gitignore 与 diff 检查通过；桌面 1280×900 和手机 390×844 浏览器回归通过。
- 发布边界：仓库只包含代码、fixture 和方案；真实第三方字幕、译文及中文音轨仍保存在被忽略的 `local-data/`，不会随 Pages 发布。
# 2026-07-15 · 更换资讯速听播报声线

范围：把公网资讯速听原型从设备相关的浏览器系统语音，替换为固定生成的自然中文音频；保留逐句高亮、连续播放和变速，不扩展多声线选择器。

成功标准：默认点击播放使用仓库内固定音频，不再调用系统语音；声线、语速和缓存参数可追溯；桌面与手机连续播放回归通过；重新发布公网地址。

- [x] 定位机械音根因 → 验证：当前 fixture 的 `audio_file` 为空，公网默认落入 `speechSynthesis`
- [x] 选择自然、低疲劳的单人声线并生成固定音频 → 验证：所有播报句都有可加载 MP3
- [x] 更新播放器降级提示与测试 → 验证：默认路径不调用系统语音，缺失音频时边界清楚
- [x] 运行单测和桌面/手机连续播放回归 → 验证：高亮、自动下一句、变速与队列推进正确
- [x] 提交、推送并验收 GitHub Pages → 验证：公网默认加载新音频
- [x] 补充 Review

### Review

- 根因：公网默认内容包没有 `audio_file`，播放必然进入设备相关的 `speechSynthesis`；功能测试通过并不能代表声线体验通过。
- 替换：默认固定为 `zh-CN-XiaoxiaoNeural / +4% / -2Hz`，四条样例共 12 个 MP3、约 528 KB；默认内容包改为与页面一起发布，避免依赖 Pages 包外的测试 fixture。
- 降级：用户导入的内容包缺少音频时仍可用系统语音，但播放栏明确显示“固定音频缺失 · 临时使用设备语音”。
- 验证：21 项单元测试通过；12 个音频路径均返回 200；桌面 1280×900 与手机 390×844 回归通过，默认音频路径没有调用系统语音。
- 边界：本轮证明跨设备声线一致和连续播放链路，不代表 8—10 分钟长听疲劳度已经通过；仍需用户真实试听确认。
- 发布：功能提交 `042179d` 已推送到 `origin/main`；公网 `briefing.json` 与首个固定 MP3 均返回 HTTP 200。
# 2026-07-15 · 资讯速听玻璃播客页视觉验证

范围：仅重做 `prototype/briefing/` 的信息布局和视觉层级，参考用户提供的 Apple TV 风格截图；保留现有数据、连续播报、收藏、原文、深挖和导入行为，不改其它课程与播放器页面。

成功标准：桌面呈现克制玻璃侧栏、清晰内容主卡与悬浮播放控制，不新增装饰图片；手机无横向溢出且主操作可达；现有核心交互和固定音频继续工作；与参考图完成同视口视觉 QA，`design-qa.md` 最终为 passed。

- [x] 明确视觉目标与功能边界 → 验证：参考截图、改版页面和用户任务均明确
- [x] 收敛视觉资产边界 → 验证：不生成或使用装饰图片，只借鉴 Apple 式玻璃层级
- [x] 重构资讯速听页面布局与玻璃材质 → 验证：桌面和手机关键层级与参考图一致
- [x] 回归队列、播放、收藏、溯源与导入 → 验证：核心任务可从头到尾完成
- [x] 完成参考图与实现截图的视觉 QA → 验证：P0—P2 清零，`design-qa.md` 为 passed
- [x] 补充 Review；本轮先本地验收，不自动推送发布

### Review

- 仅修改 `prototype/briefing/index.html`：桌面为窄玻璃队列＋大内容面板＋悬浮播放栏；手机为横向队列＋单列判断卡。
- 未使用任何生成图片；此前误生成的四张素材未接入页面并已删除。设计只借鉴参考图的空间、透明度、模糊、细边高光与黑色留白。
- 应用内浏览器验证队列切换和收藏成功，桌面 1190 × 1163 与手机 390 × 844 无横向溢出、零控制台错误。
- 21 项单元测试、Python 编译、内嵌 JavaScript 语法与 diff 检查通过；视觉对照见 `design-qa.md`，最终结果为 passed。
- 本轮按用户要求先本地验证，没有提交或推送；工作区另有无关未跟踪文件 `tools/gmail_ingest.py`，未触碰。

### 撤回记录（2026-07-16）

- 用户否定本轮玻璃风改版后，`prototype/briefing/index.html` 已精确恢复到 Git 中上一版浅色讲义风；功能与固定自然声线不变。
- 本轮临时视觉验收文件 `design-qa.md` 已删除；未提交、未推送。

# 2026-07-16 · Gmail 只读连接验证

范围：评估并补齐个人本地 Gmail → 邮件标准化 → 资讯速听内容包链路；首轮只读、仅处理用户确认的发件人或 Gmail 查询，不发送、不归档、不改标签，也不把邮件或令牌提交到 Git。

成功标准：Google 授权明确为只读；凭据与邮件落在 gitignore 目录；可用受控查询拉取少量真实邮件；邮件段落可追溯；能生成并导入现有 `briefing.json`；断开授权后不再可访问。

- [x] 审核现有页面、`tools/gmail_ingest.py`、本地产物与产品边界 → 验证：IMAP 已拉取 5 封真实邮件，并产出页面可加载的 4 条资讯、15 段固定音频
- [x] 核对 Google 当前认证与权限要求 → 验证：应用专用密码不推荐；OAuth/Gmail API 可使用更窄的 `gmail.readonly`
- [x] 用户确认继续验证 Gmail 连接器 → 验证：真实 Gmail 批次已在资讯速听页验收
- [ ] 更新 PRD 与决策日志，将 Gmail 只读连接器从“后置”调整为当前验证项
- [x] 验证真实邮件批次的数据、溯源、固定音频和页面加载 → 验证：schema 通过、15/15 MP3 存在、页面 4 条队列无报错且可切换
- [ ] 将临时 IMAP 密码方案替换为本地 OAuth，只保存被忽略的令牌 → 验证：授权、刷新、撤销均可测试
- [ ] 增加查询白名单、邮件去重、正文/HTML 提取和段落锚点测试 → 验证：fixture 单测通过
- [ ] 自动化邮件包 → 资讯速听脚本生成；当前中间 `briefing` fixture 仍为人工整理 → 验证：第二批邮件无需手改 JSON 即可生成
- [ ] 补充 Review；确认后再决定是否提交或发布代码

# 2026-07-16 · 近 7 天五个订阅源批量速听

范围：只读拉取 `aivalley@mail.beehiiv.com`、`swyx@substack.com`、`thursdai@substack.com`、`no-reply@notifications.superlinear.academy`、`noreply@bestblogs.dev` 的近 7 天来信，去重后整理为本地资讯速听批次；同时基于真实正文规模估算每日 Token，并评估 DeepSeek API 作为批处理模型的适配性。

成功标准：报告每个发件人的命中数与提取状态；同一邮件不重复；内容包通过 schema；每条事实与观点能回到邮件段落；音频与页面可加载；Token 估算给出可复算公式、日/月区间及模型成本边界。

- [x] 拉取并统计近 7 天五个发件人的邮件 → 验证：39 条原始记录，33 条去重，剔除 1 封验证码后 32 封有效邮件
- [x] 去重、筛选并生成资讯速听脚本 → 验证：收敛为 9 条/18 句，覆盖全部来源，事实/观点/AI 判断分离且有 `email_paragraph` 锚点
- [ ] 生成固定音频与页面批次 → 页面与 JSON 已返回 200；固定音频因需要把邮件摘要发送到外部 Edge TTS，等待用户知情确认
- [x] 核对 DeepSeek 官方 API 能力与当前计价 → 验证：官方文档确认 OpenAI 兼容、JSON 输出、自动上下文缓存与 V4 价格
- [x] 按现有真实 AI News 批次估算 Token 与每日/月度成本 → 验证：5 封邮件约 37,374 正文 tokens；含提示约 38,574 输入、约 891 结构化输出 tokens
- [x] 补充 Review

### Review

- 邮件覆盖：39 条原始记录，按 `message_id` 去掉 6 条重复，再排除 1 封验证码，得到 32 封有效邮件；全部提取状态为 `complete`。Superlinear 恰好达到每来源 20 封上限，仍存在截断风险。
- 内容输出：筛成 9 条高信号资讯、18 个判断句，覆盖 AI Valley、AI News、ThursdAI、Latent Space、BestBlogs 和 Superlinear；schema 与所有邮件段落锚点通过，页面和 JSON 均返回 HTTP 200。
- 根因修复：`gmail_ingest.py` 增加跨查询全局去重，避免 `swyx@…` 与 `swyx+ainews@…` 重复计费；新增 2 项测试，与资讯速听测试合计 9 项通过。
- Token：去重和验证码过滤节省约 49,378 输入 tokens；有效正文加提示约 144,705 输入 tokens，当前结构化输出约 5,694 tokens。按 DeepSeek V4 Flash 缓存未命中价估算约 US$0.02185/天、US$0.656/30 天，不含 TTS。
- 音频边界：外部 TTS 会接收邮件摘要文本；本轮未在缺少知情确认时发送，页面暂以设备语音降级。
# 2026-07-16 · 资讯速听播放时间线交互方案审视

范围：基于当前浅色讲义风页面，评估并调优“当前资讯高亮＋批次时间线＋进度条资讯总览”方案；只做设计结论，不实现。

成功标准：当前播报状态无需阅读全页即可辨认；时间线同时表达播放时间和资讯边界；用户能在一次展开内预览标题与概述并准确跳转；手机与无障碍行为明确。

- [x] 当前页面证据 → 验证：桌面、手机和主要播放状态均有截图或明确阻塞说明
- [x] 交互合理性判断 → 验证：每个保留/调整/删除项都对应具体学习或判断障碍
- [x] 推荐规格 → 验证：组件结构、状态、点击/拖动、浮层、移动端和无障碍规则完整
- [x] Review → 验证：记录证据边界、未实现项和下一轮最小验证方式

### Review

- 结论：方向合理，但必须拆开“跳转”和“展开总览”；整批时间线替换现有句数比例，正文高亮继续承担句内同步。
- 最小下一步：只改底部播放器和手机队列入口，使用现有 4 条/12 句样例验证定位、跳转、总览与继续播放，不调整已确认的浅色讲义视觉，也不扩展收藏、推荐或个性化功能。
- 交付：本轮为设计审视，无页面实现、提交或发布。
# 2026-07-16 · 资讯速听页面视觉减法复审

范围：重新审视现有浅色页面的线条密度、排版层级和播放区视觉，给出更简洁的设计方案，不改代码。

成功标准：整屏只保留具有交互意义的进度轨道；当前资讯、当前句和跨资讯入口不用横线也能辨认；手机首屏聚焦播放与正文。

- [x] 页面证据 → 验证：桌面与手机各有当前截图
- [x] 减法清单 → 验证：每个“删/留/替换”项有明确理由
- [x] 推荐方向 → 验证：布局、字体、颜色、间距和移动端规则可执行
- [x] Review → 验证：记录未实现范围和下一步验证标准

### Review

- 结论：不继续微调报刊风横线，改用留白、字重、对齐和单块轻底色建立层级；进度轨道成为常态下唯一横线。
- 推荐结构：极简页头 → 居中当前资讯 → 连续播报正文 → 轻底判断区 → 浅色悬浮播放器；资讯队列从常驻侧栏移入播放器总览。
- 下一步若实现，改动限定在 `prototype/briefing/index.html`，不触碰内容 schema、音频、课程页或连接器。

# 2026-07-16 · 资讯速听安静阅读器改版实现

范围：实现已确认的极简浅色音频阅读器，不改内容 schema、音频与其它页面。

成功标准：横线减法、无常驻侧栏、总览可达、播放功能不退化，桌面与手机回归通过。

- [x] 页面减法 → 验证：页头、栏目、句子、来源、判断区和操作区无装饰性横线
- [x] 播放器与总览 → 验证：总览可打开、切换、关闭，进度显示资讯边界
- [x] 功能回归 → 验证：播放、高亮、收藏、原文、深挖、导入保持可用
- [x] 视觉与无障碍 → 验证：390px 无溢出，焦点态、Escape、reduced motion 可用
- [x] Review → 验证：记录测试证据、截图和剩余边界

### Review

- 改动文件：`prototype/briefing/index.html`、`tests/briefing_browser_smoke.py`；任务记录与用户纠正同步更新。
- 自动验证：21 项单元测试与桌面/手机浏览器回归通过，`git diff --check` 通过。
- 视觉结果：全页常态无装饰性上下边框，唯一横向轨道为可点击的播放时间线；手机首屏可见标题、概述、三条播报与播放器。
- 边界：时间线各资讯段宽度目前按判断句数分配；要实现真实秒级比例，需要后续在内容包中保存各段音频时长。

# 2026-07-16 · 批次开场、跨资讯报幕与来源日期

范围：为资讯速听增加可见且可播的批次开场导览；在自动进入下一条资讯前播放明确报幕；在资讯元信息、队列和每条证据旁显示邮件原始日期。保持旧内容包兼容，不调整当前视觉方向。

成功标准：点击整批播放时先听到“共多少条＋主要主题”；每次自动跨条先听到“接下来第 N 条＋标题”；直接选择单条仍可正常播放；所有来源日期来自 `published_at` 且缺失时明确标记；旧 fixture、当前邮件批次和浏览器回归通过。

- [x] 扩展 schema 与 TTS 输出，支持批次开场和单条转场音频
- [x] 更新播放器为开场/转场/正文三阶段状态机
- [x] 在队列、正文元信息和证据链接显示邮件日期
- [x] 为当前 9 条邮件批次补充开场总结和 8 条转场文案
- [x] 更新单测、浏览器回归与 Review

### Review

- 播放流程变为“本批导览 → 第一条正文 → 下一条报幕 → 下一条正文”；直接点选资讯或正文句时跳过报幕，避免手动操作后重复听提示。
- 当前邮件包已生成 1 段开场和 8 段跨条报幕；开场概括 9 条内容及四组主题，转场均包含条目序号和主题。
- 当前资讯、批次总览和每个证据入口均显示来源 `published_at`；缺失时明确显示“日期未提供”，不再用批次生成时间冒充资讯日期。
- 验证：24 项单元测试、JavaScript 语法、`git diff --check`、桌面 1280×900 与手机 390×844 浏览器回归均通过。固定声线音频仍待用户授权后生成，目前使用页面标明的设备语音降级。

# 2026-07-16 · 邮件批次固定普通话音频

范围：在用户明确授权后，仅把当前批次的最终摘要文案发送给 Microsoft Edge 在线语音服务，为开场、转场和正文生成固定普通话 MP3；不发送邮件原文。

成功标准：27 段音频全部生成且可解码；内容包写入对应音频路径和时长；页面实际请求固定音频，不再降级为浏览器粤语/系统语音；桌面与手机播放回归通过。

- [x] 使用 `zh-CN-XiaoxiaoNeural` 固定声线生成 27 段 MP3
- [x] 校验文件数量、音频时长、内容包路径和 HTTP 可访问性
- [x] 验证页面不再调用 `speechSynthesis`，并完成 Review

### Review

- 已生成 27 段固定普通话 MP3：1 段开场、8 段跨条报幕、18 段正文，总时长约 356 秒；发送内容仅为最终摘要脚本，没有发送邮件原文。
- 27 个内容包路径、正时长和本地文件均完整；全部文件可解码为 MP3，并通过本地 HTTP 返回 200。
- 无头浏览器模拟完整连续播放时共请求 27 个固定音频，`speechSynthesis` 调用次数为 0，最终状态为“本批播放完成”。
- 验证：24 项单元测试、`git diff --check` 和真实邮件批次固定音频回归通过。页面刷新后不再使用可能误选粤语的系统语音。

# 2026-07-16 · 初听友好的资讯讲述结构

范围：把当前偏“事实压缩＋系统结论”的邮件短讲，调整为初听者可理解的上下文讲述；先明确内容来源或主体、背景问题和内容类型，再讲观点/趋势/原理，最后给判断与限定。保持单条可追溯和整批快速判断目标。

成功标准：未读原邮件的用户听完每条后能回答“谁在讲、这是什么、传递了什么、为什么现在值得关注”；事实、来源观点、AI 解释和 AI 判断不混淆；9 条整批控制在 15 分钟以内。

- [x] 确认单条四段式口径：主体与背景 → 内容与主张 → 原理或趋势 → 判断与限定
- [x] 按内容类型改写当前 9 条，而不是机械套用同一句式
- [x] 更新产品内容合同、fixture、证据引用和固定普通话音频
- [x] 验证整批时长、初听结构、归属标注和连续播放

### Review

- 当前 9 条均由原来的“1 段事实/观点＋1 段系统结论”改为四段式，共 36 段正文；模型、增长新闻、实时语音、Agent 工程、基础设施、硬件新闻、成本观点和 Builder 案例分别采用对应讲述逻辑。
- 每条先说明来源主体和背景，再讲内容主张；第三段解释原理或趋势，第四段给传递的信息和限定。来源内容使用 `source` 归属，通俗解释与综合判断使用 `AI` 归属，均保留邮件段落证据。
- 新版固定普通话音频共 45 段：1 段开场、8 段转场、36 段正文，总时长 691.6 秒，约 11 分 32 秒；单条正文约 65—76 秒，符合用户确认的 15 分钟批次上限。
- 验证：45 个 MP3 均可解码、时长为正并通过 HTTP 200；无头浏览器完整模拟播放请求固定音频 45 次、系统语音 0 次；24 项单元测试与 `git diff --check` 通过。闭卷复述效果仍需用户真实试听后确认。

# 2026-07-16 · DeepSeek 配置与 Gmail 定时资讯任务

范围：增加不入库的第三方模型密钥配置，使用 DeepSeek 将 Gmail 白名单邮件自动整理为资讯速听内容包；支持按本地时区设定每日运行时间；只把代码、配置模板和文档提交 Git，绝不提交密钥、邮件正文、摘要或音频。

成功标准：用户能通过本地配置选择运行时间、回看窗口、发件人和 DeepSeek 模型；定时任务执行 Gmail 拉取、去重、结构化生成、schema 校验及固定音频生成；失败有日志且不会覆盖上一份成功内容包；测试、手工 dry-run、secret 扫描、提交与 GitHub 推送通过。

- [x] 确认定时任务运行位置与私有内容访问边界
- [x] 定义 `.env`/配置模板与 DeepSeek 结构化生成协议
- [x] 实现 Gmail → DeepSeek → briefing → TTS 编排及幂等/失败保护
- [x] 实现可配置的 macOS 定时安装与日志路径
- [x] 更新测试、README、产品决策与隐私说明
- [x] 检查 secret、精准提交并推送 GitHub

### Review

- 定时任务在 Mac 本机运行，Gmail、DeepSeek 和站点密码只从 Git 忽略的 `.env` 读取；配置支持每天小时/分钟、时区、回看小时数、发件人、DeepSeek 模型、最大条数、TTS 和腾讯云目标。
- 生成流程按单封邮件调用 DeepSeek `deepseek-v4-flash`，要求四段式 JSON；本地校验 attribution、四段数量、段落编号和最终 `BriefingBatch`，按事件 slug 去重。每次使用独立批次目录，失败或没有新邮件不会覆盖上一批；`usage.json` 保存输入/输出/总 token。
- 腾讯云 `ker-cloud` 已部署当前 9 条/45 段简报。Caddy 只监听 `127.0.0.1:8792` 并提供 Basic Auth；服务器本机验证匿名 401、登录页面 200。
- 手机 390×844 模拟完整播放固定音频 45 次、系统语音 0 次。免账户 Cloudflare Quick Tunnel 创建成功但公网连续返回空响应，不能作为已验收入口；手机稳定访问仍需腾讯云安全组开放 443，或提供 Cloudflare 命名 Tunnel。
- 验证：28 项单元测试、Python 编译、launchd plist 校验、secret/ignore 检查和 `git diff --check` 通过。`DEEPSEEK_API_KEY` 当前为空，因此尚未启用每天 08:00 的 LaunchAgent，也未进行真实 DeepSeek 调用。
